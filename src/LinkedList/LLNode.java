package LinkedList;

public class LLNode {
    int value;
    LLNode next;

    // optional if the ll is a doubly linked list
    LLNode prev;

    public LLNode(int val) {
        this.value = val;
    }
}
